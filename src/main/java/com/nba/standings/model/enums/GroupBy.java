package com.nba.standings.model.enums;

/**
 * Enum representing the grouping options for NBA standings.
 * Used as a request parameter to determine how standings should be organized.
 */
public enum GroupBy {
    DIVISION,
    CONFERENCE
}
